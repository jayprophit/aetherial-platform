apiVersion: v1
kind: Secret
metadata:
  name: postgresql-secrets
  namespace: production
type: Opaque
data:
  postgres-password: $(echo -n "${POSTGRES_PASSWORD}" | base64)
  postgres-user: $(echo -n "postgres" | base64)
  postgres-db: $(echo -n "aetherial_production" | base64)
